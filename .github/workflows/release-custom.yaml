name: Release otelcol-custom

on:
  push:
    tags: ["v*"]

jobs:
  release:
    name: Release my custom Distribution of OTEL COllector
    uses: ./.github/workflows/base-release.yaml
    with:
      distribution: otelcol-custom
    secrets: inherit
